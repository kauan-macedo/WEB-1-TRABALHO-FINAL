<!DOCTYPE html>
<html lang="pt-br">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Criar Aluno</title>
		<link
			href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
			rel="stylesheet"
		/>
	</head>
	<body>
		<div class="container">
			<div class="row justify-content-center mt-5">
				<div class="col-md-6">
					<div class="card">
						<div class="card-header bg-primary text-white">
							<h2 class="text-center mb-0">Exibir Aluno</h2>
						</div>
						<div class="card-body">
							<form action="/update/alunos/<%= aluno.id_aluno %>" method="post">
								<div class="mb-3">
									<label for="nome" class="form-label">Nome Completo</label>
									<input
										type="text"
										class="form-control"
										id="nome"
										name="nome"
										value="<%= aluno.nome_aluno %>"
										required
									/>
								</div>

								<div class="mb-3">
									<label for="email" class="form-label">E-mail</label>
									<input
										type="email"
										class="form-control"
										id="email"
										name="email"
										value="<%= aluno.email_aluno %>"
										required
									/>
								</div>

								<div class="mb-3">
									<label for="curso" class="form-label">Curso</label>
									<select class="form-select" id="curso" name="curso" required>
										<% cursos.forEach((row, index) => { %> <% if(row.nome_curso ==
										cursoAluno) { %>
										<option value="<%= row.id_curso %>" selected>
											<%= row.nome_curso %>
										</option>
										<% } else {%>
										<option value="<%= row.id_curso %>"><%= row.nome_curso %></option>
										<% } %> <% }); %>
									</select>
								</div>

								<div class="mb-3">
									<label for="tel" class="form-label">Telefone</label>
									<input
										type="text"
										class="form-control"
										id="tel"
										name="tel"
										value="<%= aluno.tel_aluno %>"
									/>
								</div>
								<div class="mb-3">
									<label for="status" class="form-label">Matrícula</label>
									<select class="form-select" id="status" name="status" required>
										<% matriculas.forEach((row, index) => { %> <% if (row.estado_matricula
										== aluno.estado_matricula) { %>
										<option value="<%= row.estado_matricula %>" selected>
											<%= row.estado_matricula %>
										</option>
										<% } else { %>
										<option value="<%= row.estado_matricula %>">
											<%= row.estado_matricula %>
										</option>
										<% } %> <% }); %>
									</select>
								</div>

								<div class="d-grid">
									<button type="submit" class="btn btn-primary" id="save-btn">
										Salvar Aluno
									</button>
								</div>
							</form>
						</div>
					</div>

					<div class="text-center mt-3">
						<a href="/alunos" class="btn btn-secondary">
							<i class="bi bi-arrow-left me-2"></i>Voltar
						</a>
					</div>
				</div>
			</div>
		</div>
		<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
		<link
			rel="stylesheet"
			href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css"
		/>
	</body>
</html>
